language: objective-c
osx_image: xcode8
install:
  - carthage bootstrap --no-use-binaries
matrix:
  include:
    - script:
      - xcodebuild build-for-testing test-without-building -project Alexander.xcodeproj -scheme Alexander-iOS -sdk iphonesimulator -configuration Release
    - script:
      - pod lib lint
